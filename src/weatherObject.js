const weatherObject = ({
  description, city, country, temp, feelsLike, pressure, humidity,
}) => ({
  description,
  city,
  country,
  temp,
  feelsLike,
  pressure,
  humidity,
});

export default weatherObject;
